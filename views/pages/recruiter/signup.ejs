<!DOCTYPE html>
<html>
  <head>
    <% include ../../partials/header.ejs %>
  </head>

<body>


<div class="jumbotron">
  <div class="container">
    <a href="/" class="lang-logo">
      <img src="/lang-logo.png">
    </a>
    <h1>Welcome recruiter!</h1>
    <h2>Sign up:</h2>

    <form action="/recruiter/create_account" method="post">
      <div class="form-group">
        <label for="recruiter">Recruiter Name: </label>
        <input id="recruiter" type="text" name="recruiter">
        </br>
        <label for="company">Company Name: </label>
        <input id="company" type="text" name="company">
        </br>
        <input type="submit" class="btn btn-lg btn-primary" value="Create recruiter ID">
      </div>
    </form>
</div>
</div>

<script>
  $(document).ready(function(){
    const date_input=$('input[name="date"]'); //our date input has the name "date"
    const container=$('.bootstrap-iso form').length>0 ? $('.bootstrap-iso form').parent() : "body";
    const options={
      format: 'mm/dd/yyyy',
      container: container,
      todayHighlight: true,
      autoclose: true,
    };
    date_input.datepicker(options);
  })
</script>


</body>
</html>
